# dependencies
node_modules/

# build output
dist/

# secrets — NEVER commit (.env holds the MongoDB Atlas password + JWT secret)
.env
.env.local

# misc
*.log
.DS_Store

# note: the JSON data dir (data/) is ignored by data/.gitignore
